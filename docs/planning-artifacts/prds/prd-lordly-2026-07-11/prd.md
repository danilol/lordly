---
title: "PRD: Lord Battle Tactics"
status: final
created: 2026-07-11
updated: 2026-07-24
---

# PRD: Lord Battle Tactics

*Slug: `lordly`. Upstream: [Product Brief](../../briefs/brief-lordly-2026-07-11/brief.md) (approved 2026-07-11). Companion: [addendum.md](addendum.md) holds OB64 reference research, rejected alternatives, and architecture input.*

## 1. Executive Summary

Lord Battle Tactics is a browser-based 1v1 strategy duel that distills the automated squad combat of Ogre Battle 64 (OB64) into a five-minute match playable on a phone. Each player secretly drafts 3 units from 6 classes and places them on a 3×3 formation grid. Boards reveal simultaneously and an animated auto-battle resolves the match — all skill is spent *before* the clash, in class picks and placement reads.

**MVP:** a complete, fun match against an AI opponent in an Android mobile browser, shipped as a PWA — **delivered and performance-verified 2026-07-16 (Epics 1–3)**. **Epic 4 (committed 2026-07-16, sequenced before link-play; DELIVERED 2026-07-22):** the squad era — 5-slot armies with OB64-style monsters *(shipped 2026-07-20 as single-cell units — see the FR38 amendment)*, a 12-class roster on role-based advantage relations, tactics and squad leaders, crits and dodge, generated unit names, and battle legibility (Feature 6b). **Epic 5 (committed 2026-07-23, epic-4 retro):** content-complete roster + the medieval look (Feature 6c). **After that:** 1v1 with a friend via shareable link. This PRD states requirements so the link-play epic needs no rework (notably: a deterministic battle engine).

This is a passion project with a second agenda: the game is built AI-first through the BMAD method with no hand-written code, which elevates automated testing and documentation from hygiene to survival — the test suite is how a human verifies code they didn't write.

## 2. Goals & Success Criteria

1. **It's fun on the phone.** A full match (draft → placement → battle → result) plays smoothly in a mobile browser and the creator genuinely enjoys it. Gold signal: a friend asks for a rematch unprompted.
2. **The matchups matter** *(originally "the triangle matters"; from Epic 4 the triangle generalizes into role relations — FR14)*. Class picks and placement visibly decide outcomes; no composition dominates casual play. Counter-metric: the game must not become pure counter-picking either — placement reads (and, from Epic 4, tactic reads) must be able to beat a superior draft.
3. **The AI is worth beating.** It punishes lazy formations but is beatable with a good read. `[ASSUMPTION: a reasonable player wins roughly half their matches against it after a few games]`
4. **Match length under ~5 minutes**, bus-stop compatible. Counter-metric: the battle animation must be long enough to *watch and enjoy* — the payoff is the show. `[ASSUMPTION: battle animation lands in the 30–90 s band]`
5. **The link works cold** *(gate for the second epic, not the MVP)*: a friend with no explanation completes a 1v1 match from a bare shared URL.
6. **BMAD proves out.** Brief → PRD → architecture → epics → sprints, no hand-written code, with a test suite and documentation the creator trusts.

## 3. Players

**Primary:** the creator and his friends — strategy fans with Android phones, appetite for "one more round," zero tolerance for installs or accounts. **Secondary (if it goes public):** the OB64 nostalgia community and lapsed auto-battler players burned out on shop RNG and 30-minute lobbies. No accounts, no personal data: a player is just a browser.

### The match journey

Danilo opens the game at a bus stop. Tap **Play vs AI** → the draft screen shows six class cards; he taps Knight, Knight, Mage. Placement: he drags both Knights to the front row (left and center) and tucks the Mage in the back-center, betting the AI leads with Archers. Tap **Ready** → both boards flip face-up — the AI brought Archer/Archer/Cleric. His read was right: the reveal already tells him he's ahead. The battle plays out like a tiny OB64 clash: his Knights charge, arrows fly, the Mage's blast scorches the enemy back row; damage numbers pop. Banner: **Victory — 61% vs 18%**. He taps **Rematch** before the banner finishes animating. Total elapsed: under five minutes.

*Squad era (Epic 4, from FR1/Feature 6b): the same five-minute journey, deeper — five slots to spend, a monster looming over the grid (as shipped: the Golem on one tile, its ring of eight cells kept clear — FR38 amendment; dragons arrive with Epic 5's roster), a leader to crown and protect, a tactic to pick, and named soldiers whose stories he starts to remember.*

## 4. Functional Requirements

Requirements carry stable global IDs (FR1, FR2, …). Numbers in stat tables are **initial tuning values** — they will change during balancing without a PRD revision; the *rules* are the requirements. `[ASSUMPTION]` marks inferred decisions awaiting playtest or review.

### Feature 1 — Draft

- **FR1.** *Amended 2026-07-16 (Epic 4 "squad era" design pass; the original MVP shipped with exactly 3 units).* Each player composes an army against a **5-slot budget**, OB64-style: a **small unit costs 1 slot**, a **monster costs 2 slots** (FR38), and every army spends **all 5 slots** — so an army is 5 smalls, or 1 monster + 3 smalls (4 units), or 2 monsters + 1 small (3 units). Maximum **2 monsters** per army. Duplicates allowed. Both players always field the same slot budget. `[ASSUMPTION: the budget must be spent exactly — no undersized armies; keeps judging symmetric and drafts comparable]`
- **FR2.** The draft screen presents each class with name, sprite, and a compact rules card (role, RPS relation, per-row behavior) — enough that a first-time player can draft without a tutorial.
- **FR3.** As each unit is added to the army, it is assigned a **random element** — **Fire, Water, Wind, or Earth** — drawn from the seeded generator (FR20). The owner sees the roll immediately (before placement, so the formation can adapt to it); the opponent sees it only at reveal. A Witch's prepared spell is fixed by her element (FR16). For every other class the element is displayed but has **no combat effect in the MVP** — it is the visible groundwork for the post-MVP elemental-affinity wheel (Out of Scope). `[ASSUMPTION: elements re-roll every match, rematches included — deliberate draft variance]`

### Feature 2 — Placement

- **FR4.** *Amended 2026-07-16.* Each player places their units on their own **3×3 grid** — rows **front / middle / back**, columns **left / center / right** — via touch drag-and-drop. Any arrangement of **small units** (1 cell each) is legal. A **monster occupies two vertically adjacent cells of a single column** — Front+Middle or Middle+Back, never horizontal — and **two monsters can never share a column** (FR38, the exact OB64 deployment rule). Placement also designates the **squad leader** (FR35) and sets the army's **tactic** (FR34).
- **FR5.** Placement is **hidden and simultaneous** (Battleship-style). Neither the opponent's composition, placement — *nor, from Epic 4, their tactic (FR34) or leader (FR35)* — is visible until both players have submitted. The AI opponent must commit its choices without reading the player's (FR24). *Amended 2026-07-24 (recording story 4.13, shipped 2026-07-22): the TACTIC is the recorded exception — it is chosen (and freely changed) on the Reveal screen AFTER both boards are visible; composition, placement, and leader stay commit-blind. See FR34's matching note. PvP (Epic 6) keeps this rule symmetrically: both players see boards, then both pick tactics (epic-4 retro decision).*
- **FR6.** On both submissions, a **reveal** shows the two boards face to face before combat begins — *from Epic 4 including each side's leader marking and chosen tactic.* `[ASSUMPTION: full disclosure at reveal — an Attack Leader battle must be readable (FR39), and the reveal is where reads pay off]`

### Feature 3 — The Battle Grid: how units fight

*This section is the definitive description of targeting. It is deliberately exhaustive — it doubles as the rules reference for players, tests, and the AI.*

The two grids face each other: front rows adjacent, and each column directly faces one enemy column (a mirrored pairing — your left column faces the enemy's right, which the battle scene renders as one straight lane). "Facing column" below means that lane.

- **FR7. Reach (column adjacency).** A unit can act on enemies in its facing column and the columns adjacent to it. A **corner-column** unit reaches two enemy columns; a **center-column** unit reaches all three. Through the MVP this applies to Knights, Mercenaries, Archers, and the Witch; the Mage's row-blast and the Cleric's healing ignore reach (FR10, FR11). *Amended 2026-07-16 (Epic 4, OB64-faithful — PO-supplied rules):* **reach becomes melee-only.** Ranged and magic attackers (Archer, Witch, the Cleric's staff fallback) gain **global range** — "bows and spells do not care about lanes": they skip the column-reach filter entirely; under Autonomous they keep their row preference (rearmost-first, FR9), and under a target tactic (FR34) they ignore rows too, evaluating every living enemy. **Last Stand:** when a melee unit's reach-filtered target list is empty, out-of-reach enemies become legal targets — a battle never stalls with living units unable to act. Both changes ride Epic 4's version bump.
- **FR8. Melee targeting (Knight, Mercenary) — nearest, no bypass.** Among reachable enemies, only those in the **nearest occupied row** are eligible: a living front unit shields reachable middle- and back-row units behind it. From eligible targets, pick by priority: ① facing column, ② column closer to center, ③ left over right. Each attack re-evaluates — if the first swing kills the target, the second swing picks the next eligible enemy.
- **FR9. Ranged targeting (Archer) — rearmost first.** Among reachable enemies, only those in the **rearmost occupied row** are eligible — arrows arc over the front line to snipe the artillery, exactly the OB64 counter-logic (the Archer hunts casters — FR14). Column priority and re-evaluation as in FR8. *Amended 2026-07-16 (Epic 4, with FR7):* rearmost-first becomes the ranged unit's **Autonomous** behavior over the whole grid (no column filter); under a target tactic (FR34) row preference is ignored too.
- **FR10. Row blast (Mage).** Each cast strikes **every unit in one enemy row**, ignoring column reach. Target row = the row with the most living enemies; tie broken toward the **rearmost** row. Damage applies to each unit in the row, with RPS modifiers per target. *Amended 2026-07-14 (sprint-change-proposal-2026-07-14; mode-scoping tuned during story 3.0, PO-approved):* **in until-wipeout mode (FR19)**, each target's damage is **attenuated by the named balance ratio `blastAttenuation` (×0.75)**, applied after the base formula and before RPS — repeated engagements are where the blast compounds into dominance (the story-3.0 NFR4 sweep measured three-mages at 74.6% in un-attenuated wipeout, while single-engagement blasts are policed by the triangle: attenuating them handed the meta to archer walls at ~75%). Single-engagement blasts are unattenuated. `[ASSUMPTION: ×0.75 value and its wipeout scope — a balancing lever, verified by the NFR4 sweep in BOTH modes]` *OB64-style Archmage gating (base Mage single-target; blast unlocked by promotion) is parked; whether it enters Epic 4 wave 1 is confirmed at story breakdown (Open Items #4 — the PO's 2026-07-16 priority list did not name it).*
- **FR11. Healing (Cleric).** Each heal restores HP to the **living ally with the lowest HP percentage** (the Cleric herself included), ignoring reach, never exceeding max HP. If no ally is damaged, the Cleric instead makes a weak **STR-based staff attack** using magic targeting (rearmost reachable, as FR9) — her STR is deliberately tiny (FR15).
- **FR12. Status casting (Witch).** Each cast applies her prepared spell (FR16) to one enemy by magic targeting (rearmost reachable, as FR9), preferring targets not yet affected by that spell. She deals no damage.
- **FR13. Initiative — the Agility timeline.** Combat runs in **passes**. In each pass, every living unit that still has actions remaining (per-row counts, FR15) takes **one** action, in descending **AGI** order across both armies — the timeline weaves between the two sides, OB64-style. Multi-attack units split their attacks across passes (the "multihit split"): a front-row Knight swings once on his AGI turn, faster and slower units act, and his second swing lands on the next pass. Passes repeat until every action is spent. Ties: equal AGI → front row before back, then left before right, then a seeded coin flip across players per engagement. Dead and sleeping units lose unspent actions. Damage and effects apply immediately, in sequence. `[ASSUMPTION: class AGI values (FR15) are tuned so the natural order is Witch → Archer → Mercenary → Mage → Cleric → Knight — disruptors strike first, heals answer damage late, and the heavy tank hits last but survives to do it]`

### Feature 4 — Classes

- **FR14. Class advantage — role-based relations.** *Amended 2026-07-16 (Epic 4 design pass — Danilo: with a large roster "the triangle falls apart"; role relations replace it as the scalable form).* Every class carries a **role** (e.g. heavy melee, skirmisher, sniper, artillery, support, control, monster-brute — the wave-1 role vocabulary is an Epic 4 design deliverable). **Advantage relations are defined between roles, not classes**, as versioned balance data: a relation is either **symmetric** (attacker role deals ×1.5, reverse direction ×0.75 — today's triangle semantics) or **one-way** (×1.5 with no reverse penalty — today's `rpsHunts` semantics, which this model generalizes). A new class inherits its role's entire matchup set — the draft card teaches the role, not an N² table. The shipped triangle (Mage>Knight>Archer>Mage) and the archer's caster-hunt MUST be reproducible as the degenerate case of the role data (continuity constraint: today's 6 classes keep their effective matchups until the sweep says otherwise). Any change to relations or multipliers mandates NFR4 sweep re-verification (≤65% dominance band, both modes). `[ASSUMPTION: ×1.5/×0.75 stay the multiplier vocabulary; per-relation multipliers are a later lever if roles need finer grain]`
- **FR15. Attributes & class table.** Every unit carries the six OB64 attributes. At MVP the values are **fixed per class** — no leveling or growth; the attribute model exists so the post-MVP leveling hook slots in without a rebalance from scratch:
  - **STR** — physical damage dealt (Knight, Mercenary, Archer attacks; Cleric's staff bonk)
  - **VIT** — physical mitigation
  - **INT** — magic damage (Mage blast) and healing power (Cleric)
  - **MEN** — magic mitigation
  - **AGI** — initiative on the timeline (FR13)
  - **DEX** — accuracy, evasion, and critical hits (FR36). *Reserved through the MVP (`[ASSUMPTION: misses are too swingy in a 3-unit battle]`); Epic 4's 5-unit squads lift that condition and DEX spends its purpose.*

  Combat formulas `[ASSUMPTION — the first balancing levers]`: physical hit = `STR − VIT(target)/2`; magic hit = `INT − MEN(target)/2`; both then apply RPS (FR14), minimum 1 damage. Heal = `INT × 1.25`. All combat arithmetic is **integer math**: every division and multiplier result rounds down, in a fixed order (base → blast attenuation, Mage blast in wipeout mode only (FR10) → RPS → status modifiers) — required for FR20's tick-for-tick determinism across devices.

| Class | Role | HP | STR | VIT | INT | MEN | AGI | DEX | Actions front/mid/back | Behavior |
|---|---|---|---|---|---|---|---|---|---|---|
| **Knight** | Front-line tank | 140 | 30 | 28 | 8 | 14 | 8 | 16 | **2** / 1 / 1 | Melee (FR8); beats Archer |
| **Mercenary** | Neutral sellsword | 110 | 26 | 20 | 10 | 14 | 14 | 18 | **2** / 1 / 1 | Melee (FR8); no RPS relation |
| **Archer** | Back-row sniper | 90 | 24 | 12 | 10 | 12 | 22 | 24 | 1 / **2** / **2** | Ranged (FR9); hunts casters (FR14) |
| **Mage** | Row artillery | 80 | 6 | 8 | 30 | 22 | 12 | 14 | 1 / 1 / **2** | Row blast (FR10); beats Knight |
| **Cleric** | Support | 90 | 8 | 12 | 24 | 24 | 10 | 12 | 1 / 1 / **2** | Heal (FR11) |
| **Witch** | Control | 85 | 6 | 10 | 26 | 20 | 26 | 16 | 1 / 1 / **2** | Status (FR12) |

  The distribution follows the OB64 archetypes: heavy melee stacks STR/VIT and pays for it with the worst AGI (hits last, but survives to hit); the skirmisher Archer runs high AGI/DEX on a thin VIT frame; casters carry high INT/MEN and near-useless STR/VIT; the Witch is the fastest unit on the field so her control lands before targets act. `[ASSUMPTION: all values are initial tuning numbers, sized so a unit falls in ~3–5 neutral hits (OB64-calibrated); they live in a data file, not code, and change freely during balancing]`

  *Amended 2026-07-16 — roster wave 1 (Epic 4):* the roster grows from 6 to **12 classes: the 6 shipped classes + 4 new small classes + 2 monsters** (dragon and golem — FR38) *(as shipped 2026-07-18/20: 5 new smalls + the Golem as the only wave-1 monster, dossier D-1b — dragons arrive with Epic 5's full-roster waves)*, marching toward OB64 breadth in later waves. Strategy per the PO: **start generic, iterate to unique** — wave-1 newcomers ship as role/stat variants first and earn their signature mechanics in later iterations. The exact wave-1 class list, roles, and stat rows are an Epic 4 design deliverable validated by the NFR4 sweep; DEX stops being reserved (FR36). With the added depth, the draft rules cards (FR2) must carry each class's improved spec: role, matchups, per-row behavior, and action counts.

- **FR16. Witch spells — keyed to her element** (FR3); effects last the current engagement:
  - **Water → Sleep** (slumber mist) — target loses all remaining actions this engagement.
  - **Earth → Poison** (toxic spores) — target takes 15 damage at the end of the engagement, applied before FR18 judging. In until-wipeout mode, at the end of *each* engagement.
  - **Fire → Weaken** (searing fatigue) — target's damage is halved.
  - **Wind → Confusion** (mind-scrambling gale) — each of the target's actions has a 50% chance (seeded, FR20) to misfire. A misfired action turns on the confused unit's own side: an attack strikes a random living ally of the confused unit (a confused Mage blasts its own fullest row); a confused Cleric heals a random living enemy instead; a confused Witch applies her spell to a random living ally. If no valid misfire target exists, the action fizzles and is spent.
  - Same spell does not stack on one target; a second application is wasted. `[ASSUMPTION: the element→spell mapping is a flavor pairing, swappable during UX]`

### Feature 5 — Battle Resolution

- **FR17. Single engagement (MVP mode).** Every unit spends its per-row action count (FR15) once, in initiative order (FR13). When all actions are spent, the engagement — and the match — ends. *Amended 2026-07-23 (recording the story-4.5 device decision, 2026-07-19): the product-facing Home default is **Wipeout** (FR19), listed on the left; Standard/single remains the mode described here and the engine-level neutral fallback in `MatchFlow`.*
- **FR18. Judging.** Instant win if the enemy army is wiped. Otherwise the winner is the side with the **higher percentage of its total starting HP remaining**. An exact tie is a **draw**, with rematch offered.
- **FR19. Until-wipeout mode (stretch, ship only if cheap).** *Amended 2026-07-24: shipped since story 1.10, and the product-facing Home DEFAULT since story 4.5 (2026-07-19) — no longer a stretch; see FR17's matching note.* Engagements repeat until one side is wiped. Between engagements all statuses clear **except poison**, which persists for the rest of the battle and deals its damage at the end of every engagement (the brief's intended Witch-in-wipeout synergy). Anti-stalemate: after **10 engagements**, judge by FR18. *Amended 2026-07-16: cap raised 5 → 10 (PO: "5 engagements is too few"); the change rides Epic 4's combined version bump and mandates the NFR4 both-mode sweep re-run — poison and blast attenuation compound differently over 10 engagements.* `[ASSUMPTION: cap value]`
- **FR20. Determinism.** A battle is a pure function of (both compositions with their rolled elements, both placements, *from Epic 4 also both tactics and leader designations — FR34/FR35 are battle inputs and live in the match setup*, one random seed). The same inputs always produce the identical battle, tick for tick. All randomness (confusion, coin flips) draws from the seeded generator. *This is a product requirement, not just an engineering nicety: it makes battles unit-testable, replayable from history, and server-verifiable in the link-play epic.*

### Feature 6 — Battle Presentation

- **FR21.** The battle plays as an **animated OB64-style scene**: the two formations face off in lanes, units step/shoot/cast in initiative order, damage numbers pop, HP bars deplete, status effects show icons, deaths are visible. Indirect control is the fantasy — the player watches their plan work. The camera perspective (OB64's nostalgic isometric vs. a simpler side-on view) is a UX-phase decision; the requirement is that both grids and every action read clearly on a phone screen.
- **FR22.** The result screen declares the winner with both final HP percentages, shows both compositions, and offers **Rematch** (one tap, straight to a fresh draft) and **Home**.
- **FR23.** A battle-speed control (at minimum: normal / fast ×2) and a skip-to-result affordance. `[ASSUMPTION: watching is the payoff, but rematch grinding needs fast-forward]`
- **FR31. Art direction.** All visuals come from **free/CC fantasy pixel-art sprite packs** (itch.io, OpenGameArt, Kenney) — retro, OB64-adjacent feel with zero custom art production in the MVP. Every unit needs at minimum: idle, attack/cast, hurt, and death representations (frame animation or tween-based). License attributions are tracked in the repo and shown on an in-game credits screen.

### Feature 6b — Epic 4: the squad era + combat depth (committed 2026-07-16)

*Widened 2026-07-14 (sprint-change-proposal-2026-07-14); committed and expanded 2026-07-16 (Epic 4 design pass over Danilo's post-MVP vision — see `docs/planning-artifacts/epic-4-design-pass-input-2026-07-16.md`).* Epic 4 is a **single mega-epic with one combined `logVersion`/`balanceVersion` bump** (PO decision — one history invalidation, not several; the 3.2 UX marks stale entries non-replayable). It delivers the squad era (FR1/FR4/FR38 five-slot armies with monsters), the role-based advantage system (FR14), roster wave 1 (FR15), tactics and the leader (FR34/FR35), crits and dodge (FR36), unit names (FR37), battle clarity (FR39), the wipeout cap raise (FR19), and the systems below. The deferred `StatusCleared` events from the story-2.2 review ride the same single bump. *Determinism constraint (added 2026-07-15 from the story-3.2 replay review):* crit and hit/miss each consume a per-attack seeded `battle`-stream draw, so the design must fix their relative draw ORDER — every future seed depends on it (FR20). This is precisely what lets replay (story 3.2) reproduce crits and misses with zero per-hit storage: the seed is the recording, not the outcome. Stories still ship and get device-accepted incrementally; only the version bump is singular.

- **FR32. Per-row move variety.** For a class whose row already varies its *action count* (FR15), the row may also vary the *kind* of action taken, not just how many. Illustrative examples volunteered by the creator (not exhaustive — the full per-class table is an Epic 4 design task): a Knight in the front row throws 2× a melee "Sword Slash" (today's FR8 behavior); in the back row it instead performs a defensive move ("Shield Cover") in place of attacking. A Mage in the front row makes a weak physical "Staff Attack" instead of its row blast (mirroring the Cleric's existing no-target fallback, FR11); in the back row it casts its full row blast (FR10) at increased frequency. `[ASSUMPTION: the complete per-class, per-row move table — including Archer/Cleric/Witch variants — is undesigned; Epic 4 scoping defines it]`
- **FR33. Defensive move type ("Guard").** At least one class/row combination (e.g. back-row Knight) substitutes an attack action for a new defensive action category not present in the MVP ruleset: raising the unit's damage mitigation for the remainder of the engagement, *or* negating one incoming attack outright. `[ASSUMPTION: which of the two mechanics — or both — is undecided; this is the core new-mechanic design question for Epic 4, including its interaction with role relations (FR14) and the engine's closed BattleEvent union (AD-12)]`
- **FR34. Target-selection tactics.** At placement, each player sets their army's **tactic**, stored in `MatchSetup`: **Autonomous**, **Attack Weakest**, **Attack Strongest**, or **Attack Leader**. The engine's per-action targeting pipeline is fixed (OB64-faithful, per the PO's supplied rules; reference algorithm in the addendum): **① build the legal-target list** — melee filters by reach (FR7/FR8 blockade), with the Last Stand fallback when the filtered list is empty; ranged/magic take the whole living enemy grid — then **② apply the tactic over the legal list**: *Autonomous* = today's deterministic behavior (melee: facing column → closer to center → left; ranged: rearmost row first); *Weakest / Strongest* = lowest / highest **current HP** (absolute — OB64's rule; Strongest exists precisely to whittle down tanks and monsters first), ties falling back to the Autonomous priority (no new randomness, FR20); *Attack Leader* = the enemy leader if legal, otherwise behave as Autonomous until the path clears. A tactic never expands melee reach — it only chooses *within* it; for ranged/magic under a tactic, both column and row preferences dissolve (FR7/FR9). **Deviation from OB64, recorded:** tactics are fixed at placement for the whole battle — mid-battle switching is incompatible with the engine's resolve-once model (AD-2) and is a possible future re-architecture, not part of this epic. *Amended 2026-07-23 (story 4.13, shipped 2026-07-22): the tactic is now chosen — and freely changed — on the REVEAL screen, after the enemy board is visible; Fight! re-resolves with it (a recorded FR5/FR24 commit-blind relaxation, EXPERIENCE.md). Mid-battle switching stays deferred (post-link-play).* `[ASSUMPTION: one tactic per army, not per unit — matches OB64's squad-level order]` `[ASSUMPTION: absolute current HP per OB64; current-HP-percentage is the one-line alternate lever if absolute plays worse under the sweep]`
- **FR35. The squad leader.** Placement designates exactly one unit as **leader**. The leader anchors the Attack Leader tactic (both sides'). **Leader-killed penalty (OB64-faithful, per the PO's supplied rules):** when a side's leader dies, that side enters a penalty state for the remainder of the battle — its tactic reverts to **plain Autonomous** (the deterministic default; an OB64-style "panicked" random-targeting variant is a design option, seeded via the `battle` stream if adopted) and its units suffer a severe physical attack/defense reduction (a named, versioned balance ratio; NFR4-tuned). Named/storied hero leaders and OB64's Tamer/Master leader-monster synergy boosts (Dragon Tamer → dragons, Doll Master → golems) are explicitly parked for a later wave. `[ASSUMPTION: penalty magnitude and whether AGI/initiative leader perks exist in wave 1 are Epic 4 design decisions]`
- **FR36. Critical hits and dodge.** DEX spends its reserved purpose: an attack may **crit** (bonus damage) or be **dodged/missed** (no damage), both as seeded per-attack draws on the `battle` stream with a **fixed draw order** (see the determinism constraint above). The attack event payload carries `crit` / `missed` / `dodged` outcomes so the battle scene and log narrate them (AD-12). Magic crit rules, exact chances, and multipliers are Epic 4 design deliverables validated by the NFR4 sweep. `[ASSUMPTION: OB64 precedent — magic never crits]`
- **FR37. Generated unit names.** Every drafted unit receives a **generated name** (OB64-style), rolled exactly once at draft from a dedicated per-side seeded stream (AD-10 — a closed-set stream addition) and **stored in the match setup alongside class and element** (AD-9's rolled-values rule, per the 2026-07-16 architecture pass — storing beats re-deriving so name-table edits never touch existing history), so a replayed battle shows the identical soldiers. Names show at minimum on placement/reveal cards and battle; the name-table content is a design deliverable and lives outside the balance-hash surface. `[ASSUMPTION: names are flavor only — no gameplay effect in wave 1]`
- **FR38. Monsters (large units).** Wave 1 ships **two monster classes: dragon and golem**. A monster costs **2 slots** of FR1's 5-slot budget and physically occupies **two vertically adjacent cells of one column** (Front+Middle or Middle+Back; never horizontal; max 2 per army; two monsters never share a column — the exact OB64 deployment rule). "Monster" is a size-class, not a theme: a zombie or fairy would be a small unit. Monsters are full FR15 stat-table citizens with roles (FR14). **Two-cell-body semantics are the epic's core engine design task — BOTH directions: how a monster is targeted (row-blast counting, melee nearest-row blockade, ranged rearmost eligibility, reach from a two-cell origin) AND how it acts (which row's FR15 action count and FR32 move-kind a Front+Middle or Middle+Back body uses; how FR13's front-before-back initiative tie-break reads a unit standing in two rows). The footprint rule above is fixed; every one of these semantics must be defined before any monster story is implemented.** `[ASSUMPTION: a monster is hit once, not per-cell, by row-scoped effects — to be confirmed by the design]` *Superseded 2026-07-20 (story 4.8 device revision, PO-approved on OB64 source evidence; recorded here 2026-07-23, story 5.0):* wave 1 shipped the **Golem only** (dossier D-1b — dragons arrive with Epic 5's roster), and the monster model is **single-cell**: a monster costs 2 slots, occupies **one** cell, and **reserves all 8 king-move neighbors** (orthogonal + diagonal) at placement — no unit, human or monster, may stand in any of the 8 cells around it. There is no second body cell and no special battle targeting: the two-cell footprint and its TARGETED/ACTS semantics above are dead; a monster is a beefy one-cell participant. The old "two monsters never share a column" rule is dead too — the king-move reservation is the ONLY deployment constraint (front+back in one column is legal; max 2 monsters per army follows from the slot budget). `docs/rules.md` §Monsters is the shipped reference.
- **FR39. Battle legibility — the action ledger and turn presentation.** The battle must be readable by a player who has never seen OB64: **(a)** player-facing wording says **"Turn"** where the engine says "pass" (display rename only — the engine vocabulary and log events are unchanged); **(b)** a live **action ledger** during battle shows each unit's per-turn action economy — "Mage has 2 actions this turn → used 1 → 1 left" — with the display design owned by UX (the PO flags it as the epic's most important presentation problem); **(c)** placement surfaces per-row action counts ("Mage: back row 2×, front row 1×") so positioning is an informed choice; **(d)** attacks read as **from→to**: melee units visibly move to their target and return, and projectiles/heals/spells trace origin→target; **(e)** the redundant "front" row label is removed from the battle board; **(f)** unit labels get a contrast treatment that is legible on same-colored board tiles (shipped defect, screenshot-evidenced); **(g)** a **pause** control in vs-AI battles (playback pause — no engine impact). `[ASSUMPTION: pause is wave-1-optional if playback speed/skip already satisfies on-device review]`

### Feature 6c — Epic 5: content-complete & the medieval look (committed 2026-07-23)

*Committed at the Epic 4 retrospective + scoping session (2026-07-23; breakdown in `epics.md` §Epic 5, stories 5.0–5.10). No new FR numbers: the epic completes existing requirements — FR32/FR33's full per-class per-row move table, FR38's roster growth (humans and monsters incl. the deferred dragon, on the shipped single-cell model), FR21/FR31's art upgrade (the PO's Midjourney-generated set replacing CC packs, ONE medieval theme) — and adds two player-facing presentation surfaces, both pure reads of existing data:* **the unit-data card** *(an OB64 "UNIT DATA" modal at squad assembly: per-row moves with counts, damage-type glyph per move, stat block, HP, element, portrait — FR2/FR39c's informed-choice principle extended)* *and* **the Result battle-stats summary** *(per-unit and total damage/blocks/dodges/crits/heals/statuses folded from the `BattleLog` — FR22 extended). Closes with a pre-PvP balance verdict (NFR4) gating the link-play epic.*

### Feature 7 — AI Opponent

- **FR24.** The AI plays by the exact rules a human does: it commits a composition, placement, Witch spells — *and, from Epic 4, a tactic (FR34) and a leader designation (FR35)* — with **no knowledge of the player's hidden choices**.
- **FR25.** The AI selects from a pool of curated strategies (composition + formation archetypes) with seeded variation, so it doesn't repeat the same board every match and punishes lazy formations (e.g., it sometimes brings back-row snipers against everyone-in-front). `[ASSUMPTION: single difficulty at MVP targeting Goal 3's ~50% win rate; pool of ~8–12 archetypes at launch, validated with the NFR4 harness; difficulty tiers post-MVP]`
- **FR26.** AI decision time is imperceptible (< 1 s); it runs fully client-side and offline.

### Feature 8 — Match Flow & History

- **FR27.** Core loop: **Home → Draft → Placement → Reveal → Battle → Result → (Rematch | Home)**, completable end-to-end in under 5 minutes on a phone, no account, no tutorial gate. A compact rules/help screen is reachable from Home and Draft (content = Features 3 & 4 rules, player-worded).
- **FR28.** **Battle history:** the last **10** match results persist on-device (winner, both compositions, date, seed — *from Epic 4 the stored setup also carries each side's tactic and leader, since FR20 counts them as battle inputs and replay must reproduce them*). No backend. History screen reachable from Home. `[ASSUMPTION: storing the seed alongside compositions makes past battles replayable via FR20 — cheap and delightful]`

### Feature 9 — Platform Shell

- **FR29.** The game ships as a **PWA**: installable to the Android home screen, served over HTTPS, **fully playable offline** for vs-AI once loaded.
- **FR30.** Mobile-first **portrait** layout for phone screens (~360×640 CSS px and up); all interactions touch-native (tap, drag); a desktop browser gets a functional, centered layout without extra work. `[ASSUMPTION: portrait primary — draft cards and stacked grids fit it naturally]`

## 5. Non-Functional Requirements

- **NFR1. Performance.** Playable at a smooth frame rate (target 60 fps, floor 30) on a mid-range Android phone (~2022, e.g. Pixel 6a class) in Chrome; initial load ≤ 5 s on 4G; total bundle small enough to make that true.
- **NFR2. Quality & tests — first-class, per the creator's explicit directive.** Since no code is written by hand, the test suite is the trust anchor:
  - The battle engine is a **pure, Phaser-free TypeScript module**; every battle rule in Features 3–5 (reach, blockade, targeting priority, row counts, RPS, statuses, initiative, judging, determinism) is covered by automated unit tests — the FR text is the test spec.
  - Property-level guarantees tested explicitly: every battle terminates; judging is symmetric (swapping players swaps the result); same seed → identical battle log.
  - Golden-battle snapshot tests guard against regressions when tuning numbers change *rules* by accident.
  - CI runs the full suite on every change; a red suite blocks merge. `[ASSUMPTION: coverage gate ≥ 90% lines on the engine package]`
- **NFR3. Documentation — first-class.** The repo carries: a README that gets a stranger to a running game in minutes; a **rules document** generated from/aligned with Features 3–5 (doubles as in-game help content); architecture decision records for load-bearing choices; and doc comments on the battle engine's public API and every exported module.
- **NFR4. Balancing instrumentation.** Stats (FR15) live in a data file, not code. A headless AI-vs-AI simulation harness can sweep compositions to flag dominant strategies — cheap given FR20, and the tool for Goal 2.
- **NFR5. Privacy & compliance.** No accounts, no personal data, no tracking, no monetization. The only persistence is local device storage (FR28).
- **NFR6. Forward compatibility.** The engine API treats "two submitted boards (armies, placements — and from Epic 4, tactics and leaders) + seed → battle log" as its contract so the link-play epic swaps the AI for a remote opponent without engine changes; army size is a parameter (3 through the MVP; the 5-slot budget from Epic 4, FR1).

## 6. Out of Scope (MVP shipped 2026-07-16; list re-cut at the Epic 4 design pass)

*Promoted into Epic 4 scope on 2026-07-16 and no longer listed here: 5-slot squads and additional classes (FR1/FR15), monsters (FR38), tactics orders (FR34), squad-leader mechanics (FR35 — as a penalty state, not a rout), DEX mechanics / crits / dodge (FR36), per-row move variety (FR32/FR33).*

- Link-play 1v1 (sequenced AFTER Epic 4 — epic-3 retro decision; Node.js/WebSocket, room links, server-authoritative via FR20)
- World map, unit movement, strongholds
- **Attribute growth** — leveling, stat gains, promotions (the FR15 attribute model is their landing zone)
- **Elemental-affinity wheel** — Fire↔Water and Earth↔Wind damage interactions building on FR3's rolls. *Explicitly re-deprioritized by the PO at the Epic 4 design pass (2026-07-16): "we can delay it."*
- **Named/storied hero leaders and Tamer/Master leader-monster synergies** (Dragon Tamer → dragons, Doll Master → golems) — the wave after FR35's generic leader
- **Mid-battle tactic switching** (OB64 allows it; incompatible with AD-2's resolve-once engine — a deliberate deviation, revisit only as a fundamental re-architecture)
- Items, equipment
- Accounts, rankings, matchmaking, monetization
- Native Android packaging
- **Parries** (paired with a future DEX iteration)

## 7. Open Items

1. **Balance numbers** (FR14–FR16) are living tuning values policed by the NFR4 sweep — expected to change; not a phase blocker.
2. **Battle speed/skip default** (FR23): watch first-time vs. grind behavior in playtests.
3. **Epic 4 design deliverables** (owned by the epic's design stories, all NFR4-validated; none block PRD handoff): the wave-1 class list with roles and stat rows (FR15 — the table's descriptive "Role" column merges into FR14's formal role vocabulary here); the role vocabulary and role-relation table (FR14); **monster two-cell semantics, both targeting AND acting** *(superseded 2026-07-20 — the shipped monster is single-cell with a king-move reservation, see the FR38 amendment; no two-cell semantics exist)*; leader penalty-state numbers, whether the penalty tactic stays plain Autonomous or goes OB64-"panicked", and any wave-1 leader initiative perk (FR35); crit/dodge chances, multipliers, and the magic-crit rule (FR36); the crit-vs-dodge seeded draw ORDER (fixed forever once shipped — FR20); **tactic interaction with non-standard actions** (does Attack Leader steer the Mage's row blast toward the leader's row? does the Witch's prefer-unafflicted rule survive under a tactic? heals presumably ignore tactics — all FR10/FR11/FR12 touchpoints); the name table and name display surfaces (FR37); the per-class per-row move table and the exact Guard mechanic (FR32/FR33); **the PO's "more magic" ask** — broader spell/ability variety, whose wave-1 vehicles are the new caster classes (FR15) and the move table (FR32), with anything beyond parked to the next wave; the action-ledger display design (FR39 — UX-owned, PO-flagged as the epic's key presentation problem). **Design inputs carried from Epic 3:** the 3.0 sweep's melee-witch floor (wardens 33% single-mode — tactics should fix wasted swings), and the text-ceiling scheduling decision (Winston, hard deadline at story breakdown: does the 360px-backing fix become an Epic 4 story? Any candidate multiplies GPU fill and must verify against `docs/performance-verdict.md`'s baseline).
4. **Archmage-style promotion gating of the Mage blast** (FR10's parked item): confirm in or out of Epic 4 wave 1 at story breakdown — the PO's 2026-07-16 priority list did not name it.
5. **Sweep scale** (NFR4): the pool grows from 6 classes/10 archetypes toward 12 classes with tactics and leaders as new dimensions — the harness's archetype pool and run budget need a scaling pass inside Epic 4.

## 8. Glossary

- **Match** — one full loop: draft → placement → reveal → battle → result.
- **Battle** — the automated combat between the two revealed boards; one engagement in the MVP mode, several in until-wipeout mode.
- **Engagement** — one complete spending of every unit's actions (FR15 row counts), resolved on the initiative timeline.
- **Pass** — one sweep of the initiative timeline in which each living unit with actions remaining takes exactly one action (FR13).
- **Action** — a single attack, blast, heal, or cast.
- **Facing column** — the enemy column directly across the battlefield lane from a unit's own column (FR7).
- **Reach** — the set of enemy columns a unit may act on: its facing column plus adjacent ones (FR7).
- **Element** — Fire, Water, Wind, or Earth; rolled per unit at draft (FR3); determines the Witch's spell, cosmetic otherwise in the MVP.
- **RPS** — the class-advantage system and its damage multipliers (FR14): the triangle Mage > Knight > Archer > Mage through the MVP; from Epic 4, role-based relations with the triangle preserved as the degenerate case.
- **Judging** — deciding the winner when both sides survive: higher percentage of starting team HP remaining (FR18).
- **Seed** — the single random source a battle is replayed from (FR20).
- **Turn** — the player-facing word for a pass (FR39a); the engine vocabulary remains "pass".
- **Slot** — the draft budget unit (FR1): an army spends exactly 5; a small unit costs 1, a monster 2.
- **Monster** — the large size-class (FR38): 2 slots, two vertically adjacent cells of one column; a theme is not a size (a zombie is small).
- **Role** — a class's advantage-system tag (FR14); relations are defined between roles, not classes.
- **Leader** — the placement-designated unit whose death triggers the squad penalty state (FR35).
- **Tactic** — the army-wide targeting order set at placement (FR34): autonomous, weakest, strongest, or leader.
