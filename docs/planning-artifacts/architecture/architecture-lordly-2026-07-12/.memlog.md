---
scope: Lord Battle Tactics MVP (vs-AI PWA) + seams for link-play epic
purpose: Build substrate for AI-driven BMAD implementation (solo, no hand-written code)
altitude: initiative->features (whole system)
updated: 2026-07-12T01:24
---

- (constraint) [ADOPTED from brief/PRD] Stack direction: Phaser + TypeScript web game, PWA, mobile-first Android browser; project lives in ~/projects/js/lordly (exact versions pending web verification)
- (constraint) [ADOPTED from PRD FR20/NFR2/NFR6] Battle engine is a pure, Phaser-free TS module; battle = pure function (compositions+elements, placements, seed) -> identical result; army size parameterized; engine contract stable for link-play server reuse
- (constraint) [ADOPTED from PRD NFR4/NFR5/FR28] Balance stats live in a data file not code; only persistence is localStorage (last-10 history incl. seed); no accounts/tracking; headless AI-vs-AI sim harness required
- (constraint) [ADOPTED from PRD NFR2/NFR3] Tests first-class: engine rule coverage + property tests (termination, symmetry, seed-identity) + golden battles; CI blocks red; docs: README, rules doc (feeds in-game help), ADRs, doc comments on engine public API
- (decision) Battle resolution = REPLAY MODEL: engine resolves entire battle into immutable ordered event log (BattleLog); Phaser scene is a pure player of the log. Binds engine API + battle scene. Prevents: determinism leaking into render loop; special-case skip/replay/server paths. Chosen over live-stepped model (coaching round 1)
- (decision) Repo shape = workspaces monorepo: packages/engine (pure TS, zero Phaser dep) + apps/web (Phaser); future apps/server imports engine. Prevents: Phaser types bleeding into engine; extraction refactor at link-play epic. Chosen over single Vite app + lint fence (coaching round 1)
- (version) Web-verified 2026-07-12: Phaser 4.2.1 GA (3.90.0 is final v3, frozen; official guidance: new projects start on 4); create-game CLI targets Phaser 4 w/ Vite+TS; Vite 8.1.4; TS 7.0.2 (native compiler, brand new; 5.9/6.x zero-risk fallback); Vitest 4.1.x (not 5 beta); fast-check 4.8 + @fast-check/vitest; vite-plugin-pwa 1.3 (Workbox 7.4); pure-rand 8.4 (seedrandom unmaintained); Cloudflare Pages maintenance-mode -> Workers static assets + Durable Objects WebSocket GA free tier; pnpm workspaces idiomatic
- (decision) Phaser 4.2.x adopted for presentation layer, AMENDS brief decision (Phaser 3): v4 GA Apr 2026, official new-project guidance, official create-game scaffolds v4+Vite+TS; v3 frozen. Risk fenced to apps/web by engine purity. Chosen over Phaser 3.90 (coaching round 2)
- (decision) Hosting = Cloudflare Workers static assets (free unlimited static bandwidth); link-play epic lands on Durable Objects WebSockets (GA free tier) same platform. Prevents: platform split at multiplayer epic. Chosen over GitHub Pages / Netlify / defer (coaching round 2)
- (decision) pnpm workspaces (strict isolation protects engine fence; workspace:* protocol); TypeScript pinned 5.9/6.x line for now — TS 7.0 native compiler is 3 days old, revisit-condition: ecosystem tooling (vitest/vite plugins) confirms TS7 support. Testing: Vitest 4.1 + @fast-check/vitest (fast-check 4.x) + pure-rand 8.x seeded PRNG. [ASSUMPTION-lite: presented with lean, no user objection]
- (decision) Paradigm = functional core / imperative shell: packages/engine pure (immutable data, no I/O/clock/Math.random), apps/* own all side effects. Prevents: effectful code creeping into engine; gives AI codegen a per-file self-check. Chosen over hexagonal (coaching round 3)
- (decision) App state = Phaser Scene-per-screen FSM + single plain serializable MatchState object owned by one MatchFlow controller, passed explicitly between scenes; no state library, no Phaser registry as store. Prevents: scattered per-scene copies of truth. Chosen over zustand-style store (coaching round 3)
- (decision) Engine package owns ALL domain types (classes, elements, boards, MatchSetup, BattleLog events) AND balance data (versioned data module); web/sim/server import from engine only. Prevents: type drift / two owners of battle vocabulary
- (decision) AI opponent = pure seeded module inside packages/engine: inputs are strategy pool + seed only, never player hidden choices (enforces PRD FR24 architecturally); reusable server-side for bots
- (decision) CI/CD = GitHub Actions: every push runs typecheck+tests (red blocks merge per NFR2); main deploys to Cloudflare Workers via wrangler. Environments: production + local dev only for MVP. [ASSUMPTION]
- (decision) Persistence convention: localStorage under versioned namespace (lordly.v1.*); history entries store match setup + seed + engine balance version so FR28 replay stays valid across tuning. [ASSUMPTION]
- (question) Sim harness home: engine package dev-script (chosen, simplest) vs separate apps/sim — revisit if harness grows a UI
- (event) Reviewer gate ran: lint clean; reconcile-prd (5 gaps), rubric PASS-with-findings (R1 seed streams major), versions (Node 24 not 22, pnpm 11, TS7 GA -> explicit 5.9 pin, template ships stale deps), adversarial (6 holes, 2 critical: element-roll ownership, seed-stream contract)
- (decision) Gate fixes applied as AD-9 (elements are data in MatchSetup, shell rolls once; canonical MatchSetup shape published), AD-10 (closed named seed-stream set elements/A,B ai/A,B battle; raw seed untouchable; fresh seed per match incl rematch), AD-11 (sides A/B, human always A in vs-AI; owner-local coordinates; mirroring is renderer math; unit id side:index), AD-12 (closed versioned BattleLog event union incl misfire/fizzle/sleep/poison/pass/draw), AD-13 (MatchFlow sole engine caller + sole writer; live|replay mode; replay never writes history); AD-8 tightened (storage gateway + key manifest + BattleLog caching forbidden + balance-hash CI test); AD-5 scenes += Help, Credits; AD-7 += coverage gate + env model; AD-1 += army size is data; conventions += performance budget (NFR1) + documentation (NFR3) + seed representation; stack corrected (Node 24 LTS, pnpm 11.x, TS 5.9.x explicit pin w/ TS7-GA note, @fast-check/vitest 0.4.x, wrangler 4.x, scaffold dep-bump note)
- (event) spine finalized
