# Story 5.9 recon notes (2026-08-01 — story POSTPONED before creation)

Four recon agents ran for 5.9's create-story pass on 2026-08-01; Danilo then postponed the story
("i won't have all the sprites now. I won't have the time to generate them") per the epic's own float
rule — art stories float on batch arrival and never block the sequence (epics.md:957). The sequence
moved on to the 5.3/5.4 code reviews and 5.10. **This digest preserves the recon so the eventual
create-story run starts warm. Verify line numbers against the tree — they are 2026-08-01's.**

## The state of the sheet (7 dedicated / 20 interim)

`units.png` is 224×32 — seven 32×32 frames. Frames 0–5 DCSS CC0 (knight/mercenary/archer/mage/
cleric/witch), frame 6 Danilo's Golem. Interim sharers: berserker+phalanx→0; ninja+valkyrie+fencer+
hawkman+vultan+raven→1; dragonhunter→2; sorceress→3; ALL TEN monsters→6. AC1 needs 20 new frames →
27-frame sheet (864×32 single-row). **Append-only (frames 7–26)** keeps 0–6 stable — compositing
order IS the frame map; nothing outside sprites.ts hardcodes an index (all consumers go through
`UNIT_FRAMES`: ui.ts addUnitSprite, both 5.7 sheets, unitCard.ts:85, BootScene's guard).

## The pipeline (the 4.9 Golem precedent, canonized in 5.2)

Danilo supplies raw MJ output (gitignored `docs/planning-artifacts/ux-designs/ui/`; picks copied to
tracked `…/midjourney/selected/` — the ORIGINAL 12 classes' sprites are already there and complete) →
agent keys near-white→transparent with sharp (transitive dep, allowlisted), crops to content,
downscales →32×32, composites onto the sheet, +32 width per frame. Raw masters to gitignored
`art-src/`. Portraits differ: prompt pack **§5** (NOT "section 2" — epics.md:1157 carries that drift;
recorded at 5-6:18), dark background, NOT keyed, opaque squares, one per CLASS (27), fixed 64px
render (UNIT_CARD.portraitW; never loomed — the 5.6 de-loom decision, DESIGN.md:291 pins "FIXED 64px").

## Portrait swap points (designed in 5.6 to be one lookup)

`unitCard.ts:85` (`portraitFrame: UNIT_FRAMES[cls]` — doc names 5.9), `unitCardOverlay.ts:40` (texture
key, currently UNITS_SHEET_KEY), `unit-card.test.ts:22` (the pin that deliberately breaks). A separate
portraits texture needs its OWN BootScene load + `setFilter(NEAREST)` if pixel-art (BootScene filters
only the units sheet; never global pixelArt — device-confirmed 2.1). The 5.7 stats/summary sheets stay
BOARD sprites (spine pins them as sprites; they improve automatically via UNIT_FRAMES). Portraits at
≥64px native, likely 128 for DPR (the canvas-backing lesson).

## The 4.3 defer, corrected

deferred-work.md:152's citation drifted: the constant is now `SHEET_FRAMES = 7` at
**sprites.test.ts:20** (not :695, not =6); the target is 27 (not 11). Three things encode sheet size
with nothing linking them: the PNG width, SHEET_FRAMES + the `dedicated` list (sprites.test.ts:20-23),
and BootScene.ts:80's guard (self-derives from UNIT_FRAMES but is a LOWER bound only — a mis-sliced
over-long sheet boots silently; no mechanical content check exists for units.png — the device pass is
the content gate, per the art-story split). Close the defer by DERIVING the constant, not re-hardcoding.

## Attribution (AC2) — retire vs survive

attribution.ts is at `apps/web/src/assets/attribution.ts` (not config/). Three entries: DCSS
(units.png frames 0–5 + 16 human classSources rows, 10 marked INTERIM), Lordly-original (golem frame 6
+ 10 INTERIM monster rows), Lordly-Midjourney (chrome/backgrounds/terrain/wordmark/icons — survives
wholesale). DCSS dies entirely IF frames 0–5 are also replaced (Danilo's pick). attribution.test.ts's
load-bearing gates: assets-exist-on-disk, license allowlist, **every class traces to a pack** (the
overhaul can't delete classSources), and the Golem no-INTERIM pin — the template for "NO class is
INTERIM anymore". Riders for the same touch: `flow/credits.ts:27` derives Supplies: from classSources
only → add a non-sprite `supplies` field (deferred :203); CreditsScene:34's "free work" tagline reads
stale; sprites.ts/BootScene stale prose ("seven frames", "ships INLINED as data-URI" — already false
at 5,780B > 4,096 inline limit, definitively false at 27 frames).

## Already resolved / adjacent

- **The PWA-icon provenance note (deferred :29) is ALREADY substantively resolved** — 5.2 regenerated
  the icons from the MJ master and moved the attribution lines (5-2:37/:44; attribution.ts:36-38,
  :122-125). 5.9's job is only to strike the ledger entry RESOLVED-in-5.2.
- Still genuinely open for 5.9: wordmark.jpg→PNG re-export (deferred :200 — note 5.8's
  PANEL_FRAME_SLICE 30→46 partially fired its "may resolve with the re-crop" condition; re-judge on
  device), PWA icon ground #142637 vs manifest #161a2e (deferred :202).
- The inline-Draft-panel wish (deferred :269-274) counts on 5.9's portraits — sequencing neighbour,
  NOT scope.
- Perf: 5.9 adds real texture weight; 5.10's `?perf=1` capture is supposed to cover the full-roster
  sheet (epics.md:1181-1183) and the Battle scene-ENTRY burst. **Ordering note recorded 2026-08-01:
  5.10 will now run BEFORE 5.9**, so its capture precedes the final art — either a light re-capture
  rides 5.9's device pass, or the gap is recorded in performance-verdict.md.

## New-art hazards the story must carry

Side-colour neutrality (the Golem was drawn side-neutral to dodge blue=you/red=enemy — every new tile
needs the same check); monster tiles must compose for the LOOMED read (×1.5 = 48px from a 32 base);
the Whelp is a monster class that renders SMALL (sizeClass drives loom, automatic); per-texture
NEAREST on any new sheet; frame-index order = compositing order.
